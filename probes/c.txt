[ executable ]

Trivial C executable.

/__init__.py
	#!/pl/python
		__factor_domain__ = 'system'
		__factor_type__ = 'executable'

		expected = "simple text"

/src
	/program.c
		#!/pl/c
			#include <stdio.h>

			int
			main(int argc, char *argv[])
			{
				int i = 5;
				printf("i=%d\n", i);
				if (argc > 1)
					printf("a=%s\n", argv[1]);

				return(200);
			}

[ library ]

Trivial library.

/__init__.py
	#!/pl/python
		__factor_domain__ = 'system'
		__factor_type__ = 'library'

/src
	/main.c
		#!/pl/c
			int
			exported_symbol(int args, char *argv[])
			{
				int i;
			}

[ extension ]

Trivial dynamically loaded library checking for symbol presence and hiding.

/__init__.py
	#!/pl/python
		__factor_domain__ = 'system'
		__factor_type__ = 'executable'

/src
	/main.c
		#!/pl/c
			int
			main(int args, char *argv[])
			{
				int i;
			}
